[pytest]
# The DSP engines are plain modules at the repository root, so the project
# root has to be importable for `import smuve_filter` etc. to resolve.
pythonpath = .
testpaths = tests/dsp
addopts = -q
filterwarnings =
    error::RuntimeWarning
