import json
import subprocess

def call_tool(tool_name, params):
    # Simulated call for listing comments on the current project/workspace
    print(f"DEBUG: Searching comments via {tool_name}")

# Try to list recent comments in the team SMUVE
