/**
 * user service stub.
 *
 * The real TypeORM-backed user CRUD/migrations live in server/index.js
 * for now. This file exists only so `src/services/index.ts` (the barrel
 * re-export used by `src/socket/index.ts`) compiles cleanly. Once the
 * server migrates fully into src/, replace this stub with the real module
 * exporting `getUserById`, `createUser`, `updateUserProfile`, etc.
 */
export {};
export default {};
