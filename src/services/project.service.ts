/**
 * project service stub — see src/services/user.service.ts.
 * Real studio project CRUD + branches live in server/index.js. Full
 * migration lands in a follow-up and will plug into the live
 * collaboration packet / lineage APIs.
 */
export {};
export default {};
