/**
 * storage service stub — see src/services/user.service.ts.
 * Real R2/S3 upload presign + headroom lives in server/index.js (the
 * @aws-sdk/client-s3 path); full migration lands in a follow-up.
 */
export {};
export default {};
