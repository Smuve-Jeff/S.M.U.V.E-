import { Router } from "express";

/**
 * profile routes stub — see src/routes/auth.routes.ts.
 * Mounted at `/api/profile` in src/app.ts.
 */
const router: Router = Router();

export default router;
