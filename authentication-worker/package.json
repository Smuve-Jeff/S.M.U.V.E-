{
	"name": "authentication-worker",
	"version": "1.0.0",
	"description": "Cloudflare Worker for authentication",
	"main": "src/index.ts",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"test": "vitest run",
		"typecheck": "tsc --noEmit -p test/tsconfig.json && tsc --noEmit"
	},
	"dependencies": {
		"jose": "^5.0.0"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.0.0",
		"typescript": "^5.0.0",
		"vitest": "^3.2.0",
		"wrangler": "^3.0.0"
	}
}
