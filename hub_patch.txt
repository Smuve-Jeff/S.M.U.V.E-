<<<<<<< SEARCH
                <div class="flex items-center justify-center md:justify-start gap-6">
                   <button (click)="playerService.previous()" class="text-silver-dim hover:text-white transition-all"><i class="fas fa-step-backward text-xl"></i></button>
                   <button (click)="playerService.togglePlay()" class="w-14 h-14 md:w-16 md:h-16 rounded-full bg-white text-black flex items-center justify-center hover:scale-110 active:scale-95 transition-all shadow-xl shadow-white/5">
                      <i class="fas text-xl md:text-2xl" [class.fa-play]="!playerService.isPlaying()" [class.fa-pause]="playerService.isPlaying()"></i>
                   </button>
                   <button (click)="playerService.next()" class="text-silver-dim hover:text-white transition-all"><i class="fas fa-step-forward text-xl"></i></button>
                </div>
=======
                <div class="flex items-center justify-center md:justify-start gap-6">
                   <button (click)="playerService.previous()" class="text-silver-dim hover:text-white transition-all"><i class="fas fa-step-backward text-xl"></i></button>
                   <button (click)="playerService.togglePlay()" class="w-14 h-14 md:w-16 md:h-16 rounded-full bg-white text-black flex items-center justify-center hover:scale-110 active:scale-95 transition-all shadow-xl shadow-white/5">
                      <i class="fas text-xl md:text-2xl" [class.fa-play]="!playerService.isPlaying()" [class.fa-pause]="playerService.isPlaying()"></i>
                   </button>
                   <button (click)="playerService.next()" class="text-silver-dim hover:text-white transition-all"><i class="fas fa-step-forward text-xl"></i></button>

                   <div class="flex items-center gap-2 ml-4 border-l border-white/10 pl-6">
                      <button (click)="playerService.loadExternalTrack()" title="Upload Track" class="w-10 h-10 rounded-full bg-brand-primary/10 border border-brand-primary/20 text-brand-primary flex items-center justify-center hover:bg-brand-primary hover:text-white transition-all">
                         <i class="fas fa-cloud-upload"></i>
                      </button>
                      <button (click)="audioEngine.setOutputMode(audioEngine.outputMode() === 'speakers' ? 'headphones' : 'speakers')"
                              [title]="audioEngine.outputMode() === 'speakers' ? 'Switch to Headphones' : 'Switch to Speakers'"
                              class="w-10 h-10 rounded-full bg-white/5 border border-white/10 text-silver-dim flex items-center justify-center hover:bg-white/10 transition-all"
                              [class.text-brand-primary]="audioEngine.outputMode() === 'headphones'">
                         <i class="fas" [class.fa-volume-up]="audioEngine.outputMode() === 'speakers'" [class.fa-headphones]="audioEngine.outputMode() === 'headphones'"></i>
                      </button>
                   </div>
                </div>
>>>>>>> REPLACE
